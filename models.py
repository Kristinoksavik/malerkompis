"""
app/models.py — Datamodeller
Foreløpig ikke i bruk — klar for fremtidig utvidelse.

Eksempler på hva som kan legges her:
- Brukermodell (hvis du legger til database)
- Samtalehistorikk (hvis du vil lagre chat)
- Fargepaletter lagret av brukere
"""

# TODO: Legg til SQLAlchemy eller lignende hvis du trenger database
# Eksempel:
#
# from flask_sqlalchemy import SQLAlchemy
# db = SQLAlchemy()
#
# class User(db.Model):
#     id = db.Column(db.Integer, primary_key=True)
#     username = db.Column(db.String(80), unique=True)
#     ...
